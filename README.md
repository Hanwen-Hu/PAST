# PAST
PAST: Primary-Auxiliary Spatio-Temporal Network for Traffic Time Series Imputation

```
pip3 install -r requirements.txt
python3 main.py -dataset MeTr-LA -miss_rate 0.4 -miss_len 48 -miss_span 5
```
